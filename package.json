{
  "name": "dnms-checkin",
  "version": "1.0.0",
  "private": true,
  "description": "DNMS Check-in app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "http-server": "^14.1.1"
  }
}
